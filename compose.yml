
version: '3.8'

services:
  db:
    image: postgres:16
    container_name: metro_postgres_db
    restart: always
    environment:
      # Bariables
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Brackix123
      POSTGRES_DB: metroapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
